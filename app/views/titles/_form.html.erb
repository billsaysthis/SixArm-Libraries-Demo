<%= form_for(@title) do |f| %>
	<%= render "shared/error_messages", :target => form %>

  <div class="field">
    <%= f.text_field :title %>
  </div>
  <div class="actions">
    <%= f.submit %> <%= link_to 'Cancel', titles_path %>
  </div>
<% end %>
